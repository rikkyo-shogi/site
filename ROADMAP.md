# 立教大学将棋部サイト 発展ロードマップ & コンテンツアイデア集

> 位置づけ：本ドキュメントは「将来こうしていきたい」を検討するための**前向きな計画メモ**。
> 確定した要件・データ仕様・実装状況は従来どおり **`requirements.md` が唯一の正**（§0 実装状況 / §5 フィールド定義 / §11 SEO）。ここで固まった項目は requirements.md 側に反映していく運用にする。
>
> ゴール：現在の「大会結果 + 掲示板アーカイブ」サイトを、**立教将棋部関係者（現役・OB・OB会）のハブ**へ育てる。

---

## 0. 現状（この構成の上に積む）

既に土台はかなり整っている。新機能は「作り直し」ではなく**この設計の踏襲・拡張**として設計する。

| 層 | 現状 | 置き場所 |
|---|---|---|
| データ | 年度別JSON（H21〜R08 / 77イベント）、`kanto_table`・`rikkyo_result`・`bbs_detail`・`schedule` を構造化 | `data/confirmed/*.json` |
| スキーマ | Draft7 + CI検証 | `data/schema.json` / `.github/workflows/validate.yml` |
| 取り込み | Python 半自動スクレイパー（関東PDF/HTML/XLSX・掲示板） | `scraper/*.py` |
| サイト | Astro SSG（`output:'static'` / `base:'/site'`）。メタは `BaseLayout.astro` に集約 | `site/src/**` |
| ページ | トップ / 大会結果 / 掲示板アーカイブ | `site/src/pages/**` |
| 部品 | `SeasonSection` `TeamEvent` `IndividualEvent` | `site/src/components/**` |
| データ読込 | 型定義つきローダ | `site/src/utils/loadData.ts` |
| 公開 | GitHub Pages（push で自動デプロイ） | `.github/workflows/deploy.yml` |

**設計原則（既存を踏襲）**
- データはHTMLに直書きせず**JSONで持ち、Astroがビルド時に描画**する（現行どおり）。
- 一次情報はPDF。**半自動（抽出→JSON→目視でconfirmed化）**を維持。ダミーデータで進めない（§1.5.1）。
- メタ情報は各ページに書かず **`BaseLayout` のprops**へ。URLは `import.meta.env.BASE_URL` / `Astro.site` 経由（`base:'/site'` ハードコード禁止）。
- 新しい `source_type` を使うなら **`schema.json` の enum と requirements.md §5 を更新**。

---

## 1. 優先順位付きロードマップ

| Phase | 機能 | 一言 | 難易度 | 既存資産の再利用 |
|---|---|---|---|---|
| **1** | 社団戦 チーム結果・過去結果 | まず載せたい① | 中 | `kanto_table`型・`TeamEvent`・スクレイパー流用 |
| **1** | 社団戦 個人レーティング推移 | まず載せたい② | 中 | 公式レーティング＋登録番号（安定ID）を確認済み（§2-2） |
| **2** | 現役 団体戦 昇降級推移グラフ | 既存データで即・映える | **低** | `data/confirmed` をそのまま集計 |
| **2** | 成績DB / 個人成績検索 | 蓄積を「使える」に | 中 | 既存＋社団戦データ |
| **3** | 部の歴史 年表 | ハブの読み物 | 中（編集主体） | 掲示板＋大会結果 |
| **3** | OB会 活動紹介（公開分） | OB会の可視化 | 低 | 静的ページ |
| **4** | 関係者限定エリア | note/予算/報告/棋譜 | 低（実装ゼロ） | Googleグループ＋Drive委譲で確定（§4） |
| **4** | イベントカレンダー | 予定の一元化 | 低 | Googleカレンダー埋込 |
| **4** | 大会棋譜アップロード・閲覧 | 棋譜の資産化 | 中 | 保存先＋ビューア |

> **おすすめの入り方**：**昇降級グラフ（Phase 2）は既存データだけで作れて映える**ので、社団戦（Phase 1）と並走させると早く「成果」が見える。

---

## 2. 各機能の実装方針（現アーキテクチャに載せる形）

### 2-1. 社団戦 チーム結果・過去結果 ★Phase 1
- **事実確認済み（第34回・順位一覧PDF）**：立教のチームは**「立教大学紫龍会」（3部白 16位・4部へ降級）と「紫龍会」（5部赤 9位）の2チーム**。**単一チーム前提の設計は不可**。回によってチーム数・名称・所属部は変わり得るので、取り込みのたびに確認する。
- **データ源**：東将連 `shadan/sub2.htm` → 各回ページ `sub9-RXX.htm` → **リーグ表/一覧/ランキングPDF**（例：第34回=令和7年度）。
- **データ設計**：関東と混ぜず**別名前空間**にする。
  - 置き場所：`data/shadan/confirmed/RXX.json`（年度別）。
  - **`teams[]` 配列でチーム単位に持つ**：`{ team_id, team_name, kai(第N回), division(部: 1〜7部・赤白), rank, points, wins, promotion, league_table(rows[]) }`。`team_id` は「立教大学紫龍会」「紫龍会」を安定IDで区別（将来の名称変更に備える）。
  - 新 `source_type: "shadan_pdf"` を追加 → **`schema.json` enum と requirements.md §5 を更新**。
- **スクレイパー**：`scraper/fetch_shadan.py` + `scraper/parse_shadan.py` を新設（既存 `fetch_kanto.py` / `parse_team.py` を下敷きに）。`pdfplumber` で表抽出 → 対象2チームの行を特定 → JSON。
- **表示**：`site/src/pages/shadan/index.astro` を新設。既存 `TeamEvent` を社団戦向けに一般化 or 派生。2チームを並列表示。出典PDFリンクは必ず付す（現行の作法）。
- **注意**：所属部（1〜7部・赤白）で強さの文脈が変わるので**部の階層を必ず記録**。PDFレイアウトは回で変わる前提で半自動＋目視。

### 2-2. 社団戦 個人レーティング推移 ★Phase 1
- **事実確認済み（第34回・3部白ランキングPDF）**：部別ランキングPDFの列は**順位 / 登録番号 / 氏名 / 新持点（レーティング） / 通算対局数 / チーム名**。
  - **公式レーティング（新持点）がそのまま取れる**。代理指標や独自Elo計算は不要。
  - **登録番号が年をまたぐ安定ID**。懸念だった名寄せ（同姓同名・改姓）は登録番号キーでほぼ解決。
  - PDFは**部（リーグ）単位**。立教2チームの所属部の分を取得する（チームの所属部は毎年動く点に注意）。
  - **公開されていない年度がある**。欠測前提の時系列設計にし、グラフでは欠測区間を明示する（線で埋めない）。
- **データ設計**：`{ reg_no, names[], team_ids[], history: [{ kai, season, division, rating, games }] }` の時系列（個人単位）。
- **置き場所（§4-0の帰結・重要）**：**このリポジトリはpublicなので、個人の時系列JSONはリポジトリにコミットしない**（ページ非表示でもコミット＝公開）。公開範囲が決まるまでは、抽出スクリプトの出力先をリポジトリ外（ローカル→§4のDrive保管）とする。**同意を得て公開サイトに載せると決めた人の分だけ** `data/shadan/players/` にコミットする。
- **表示**：一覧 → 個人ページ、`Chart.js` で推移の折れ線（公開同意者のみ）。
- **個人情報の方針（実装より先に決める）**：氏名・レーティングは公開PDF由来だが、**経年で集約しプロフィール化するのは元の公開より侵襲的**。デフォルトは**関係者限定側（§4のDrive）に置き、公開サイトには本人同意を得た人のみ掲載**。登録番号は内部キーに留め、ページには表示しない。

### 2-3. 現役 団体戦 昇降級推移グラフ ★Phase 2（最短で成果）
- **データ源**：**既存 `data/confirmed/*.json`**。`kanto_table`（team=立教の行）と `rikkyo_result.promotion`、`division` が既に揃っている。
- **実装**：
  1. `loadData.ts` に集計関数を追加（`type:'team'` かつ立教のイベントを時系列化 → `{season, division, rank, promotion}`）。
  2. 級を数値化する**正規化テーブル**（例：C2=1, C1=2, B2=3, B1=4, A=5…）。※年代で「C級」等の表記差があるので変換表を1枚。
  3. `Chart.js` の stepped line で階段表示。昇級▲・降級▼・優勝★のマーカー。
  4. 置き場所：新コンポーネント `site/src/components/LeagueTrend.astro`（Chartはクライアントside script）。トップ or 大会結果ページのアイキャッチに。
- **メモ**：本ロードマップ添付のデモ（H21〜R07のリーグ往復グラフ）はこの方針の試作。**追加データ収集ゼロ**で作れる。

### 2-4. 成績データベース / 個人成績検索 Phase 2
- **やること**：現役・OBの個人ベスト成績、対戦相手校別の通算成績などを検索・集計。
- **実装**：既存 `rikkyo_players` / `kanto_table` を横断集計。まずは静的な集計ページ（ビルド時生成）で十分。全文検索は `Pagefind`（静的サイト向け・Astro相性良）を検討。

### 2-5. 部の歴史 年表 Phase 3
- **データ源**：掲示板アーカイブ（`bbs_detail`・cloudfront過去ログ）＋大会結果＋OBヒアリング。
- **実装**：`data/timeline.json`（`year/month/title/body/tags/link`）→ 年表コンポーネント。骨組み（判明済みの大会実績・昇降級）は既存JSONから**自動生成**し、エピソードは追記運用。OB寄稿の受け皿にもなる。

### 2-6. OB会 活動紹介（公開分）Phase 3
- **実装**：まず静的ページ1枚（`site/src/pages/obog/index.astro`）。OB会とは/年間行事/連絡・入会導線。非公開の詳細（予算・名簿）は限定エリアへリンク（§4）。

---

## 3. 棋譜・カレンダー等（Phase 4 の実装メモ）

- **棋譜ビューア**：`KIF/CSA` を `kifu-for-js` でブラウザ再生。**公開可の棋譜のみ** `data/kifu/` に置く（リポジトリはpublic・§4-0）。非公開棋譜は§4のDrive側。大会の名局を「並べられる」形に。
- **イベントカレンダー**：Googleカレンダーの埋め込みが最安。例会・大会・OB会行事を一元化。
- **棋譜アップロード**：静的サイトには投稿機能がないので、**フォーム→ストレージ**（Googleフォーム＋Drive、あるいは限定リポジトリへPR）で受け、確定分をサイトに反映する半自動運用が現実的。

---

## 4. 関係者限定コンテンツ — 方針（2026-07-18 全面改訂）

### 4-0. 動かせない前提（ここから逆算する）

1. **リポジトリ `rikkyo-shogi/site` は public**（確認済み）。コミットした内容は、サイトのURL設計と無関係に**GitHub上でソースごと全世界公開**され、削除してもgit履歴に残る。
2. GitHub Pages（無料）は public repo が前提。private化はプラン変更＋構成見直しが必要で、現時点でその理由がない。
3. よって**大原則：限定コンテンツ（後述L2以上）は、このリポジトリと GitHub Pages に一切置かない**。「置いた上で隠す」方式はすべて不成立。誤コミット自体を事故として扱う。

### 4-1. 脅威モデル（何から・何を守るか）

**守る相手**（現実的な順）：
1. 検索エンジン・AIクローラ・アーカイブ（Wayback等）による恒久的な収集・複製
2. 通りすがりの部外者（URLを偶然・興味本位で辿る人）
3. リンク転送・SNS転載による意図しない拡散（LINE/Discordがプレビュー生成時にURLを取得する経路を含む）

標的型攻撃は想定しない（資産の価値と釣り合わない）。リスクの本体はハッキングではなく、**(a) クローラへの恒久露出 (b) 人手による転送拡散 (c) 個人情報の管理不備**の3つ。

**守る資産**：
- **L0** 公開してよい：活動予定・公開レポート → 通常公開（限定不要）
- **L1** 検索に載せたくない・身内向け：非公開noteリンク集、イベント詳細
- **L2** 部外者に見せない：予算資料、内部向け活動報告、棋譜の一部
- **L3** 個人情報：名簿、連絡先、同意のない個人成績の集約（§2-2）

### 4-2. 採用する方式（結論）

**「認証は Google に委譲し、公開サイトは公開ハブに徹する」の一本。** 段階案は取らない。

1. **OB会/部の共用Googleアカウント + Googleグループ「関係者」を開設**。個人アカウント名義にしない（属人化防止）。管理者は常に2名以上。
2. **共有フォルダ（Drive）をグループ限定公開**にし、L1〜L3をすべてここに集約：
   - L1：非公開noteリンク集・イベント詳細 → グループ限定のGoogleドキュメント
   - L2：予算・内部報告・非公開棋譜 → 同フォルダのサブフォルダ
   - L3：名簿・連絡先 → さらに権限を絞ったサブフォルダ（役員のみ等）＋ダウンロード制限
3. **公開サイトには「関係者向けページ」への入口リンクを1つ置くだけ**。リンク先はDrive。開けるかはGoogle側の権限が決めるので、**入口リンク自体は公開されても無害**（sitemap・プレビュー・転載を気にする必要がなくなる）。
4. **棋譜アップロード**：Googleフォーム → Drive自動保存。公開可のものだけ人手で confirmed 化してリポジトリ → サイト掲載（既存の半自動運用と同じ思想）。
5. **カレンダー**：公開予定は公開Googleカレンダーをサイトに埋め込み。内部予定はグループ限定カレンダー。

利点：実装ゼロ・無料・**個別失効が可能**（卒業/退会時にグループから外すだけ）・リンクが漏れてもGoogleの認証が最後の砦・スマホからも普通に使える。

### 4-3. 必須の運用ルール（技術選定より重要）

アクセス制御の本体は**会員ライフサイクル管理**。これを決めずに基盤だけ作らない。

- **入退会フロー**：グループへの追加/削除を誰が承認・実行するか（例：OB会幹事）。申請導線（フォーム）も1つ用意。
- **年1回の棚卸し**：卒業者・退会者・連絡不能者の削除。時期を固定（例：新年度）。
- **引き継ぎ**：共用アカウントの認証情報は幹事交代時に必ず引き継ぐ。管理者2名以上を維持。
- **最小権限**：L3は「必要な人だけ」。全員に開く理由がない資料をL2に混ぜない。
- **リポジトリ側の規律**：`data/` 以下に非公開系ディレクトリを作らない。「.gitignoreで守る」ではなく「置き場を作らない」。

### 4-4. 採用しない方式と、その理由（再検討条件つき）

| 方式 | 不採用の理由 | 再検討するなら |
|---|---|---|
| 限定URL（unlisted + noindex） | public repoでソースごと公開になり無意味。仮に解決してもsitemap自動生成・SNSプレビュー・Refererから漏れ、一度クロールされたら恒久露出 | 再検討しない（L1もDrive側で足りる） |
| staticrypt等のクライアント側暗号化 | 平文原稿がrepo履歴に残る。共有パスワードは個別失効が不可能 | 再検討しない |
| Cloudflare Access | **現構成では防御にならない**（オリジン `rikkyo-shogi.github.io` が素通しでバイパス可能）。成立させるにはrepo私有化＋Cloudflare Pages等への移行＋ユーザー管理（無料50席上限）が必要で、Driveに対する追加の利得が不明 | 「会員向けコンテンツを**Webページとして**見せたい」具体的需要が出て、移行コストを払う体制ができたとき |
| Netlify Identity等への移行 | 同上。いま解くべき問題に対して過大 | 同上 |
| note等の限定公開を「認証」として扱う | note非公開は機構的には限定URLと同じ。noteで完結する記事はそれでよいが、安全装置とは見なさない | — |
| メール/LINEでの資料配布のみ | 実は有力な代替だが、過去資料の再取得性が低い | **併用する**：「配布はメール・保管はDrive」 |

---

## 5. 追加コンテンツのアイデア集（★＝費用対効果が高い）

**記録・データ系**
- ★ 部の通算成績ダッシュボード（通算勝敗・最高到達リーグ・優勝回数）
- ★ 対戦相手校データベース（「立教 vs ○○大学」歴代成績・ライバル校ページ）
- 個人戦績アーカイブ（現役・OBのベスト成績名鑑／公開可否は本人選択）
- 段位・棋力の記録（アマ段位・県代表・全国出場・奨励会歴）
- 「今日は何の日」（年表データの再利用）
- 統計で見る立教将棋部（戦法の流行・世代交代の可視化）

**棋譜・観る将系**
- ★ 棋譜ビューア（kifu-for-js）／名局選・大逆転譜／詰将棋コーナー（新歓広報にも効く）／得意戦法図鑑

**コミュニティ・読み物系**
- ★ OB寄稿ブログ・引退エントリ（ハブの「体温」）／現役の週報・大会レポ（note連携可）／レジェンド・追悼ページ／写真アーカイブ（限定可）／世代を超えた座談会

**つながり・運用系**
- ★ イベントカレンダー（Googleカレンダー埋込）
- ★ 連絡導線の集約（現役=LINE/Discord、OB会=メーリス/フォーム）
- 新歓・勧誘ページ（活動時間・場所・見学フォーム）＝部の生命線
- OB名簿（限定・年代別/卒年別）／OB会費・寄付導線／掲示板アーカイブの全文検索／お知らせ・RSS

**あると気が利く系**
- 部歌・部訓・ロゴ等の公式アセット集／アクセスマップ／FAQ（現役向け・OB向け）／リンク集（関東連盟・全日本学生連盟・東将連・他大学）

---

## 6. 技術メモ（既存スタックに足すもの）

| 領域 | 追加候補 | 備考 |
|---|---|---|
| グラフ | **Chart.js**（軽い） | 昇降級・レーティング推移。クライアントscriptで |
| 全文検索 | **Pagefind** | 静的サイト向け。Astroと相性良 |
| 棋譜 | **kifu-for-js** | KIF/CSA再生 |
| 社団戦取込 | `scraper/fetch_shadan.py` `parse_shadan.py` | 既存スクレイパーを下敷き。`pdfplumber` |
| データ | `data/shadan/**` `data/timeline.json` | 関東と名前空間を分離。schema.json拡張 |
| 限定資料 | Googleグループ＋共有フォルダ | 認証はGoogleに委譲（§4）。サイト側の実装なし |
| カレンダー | Googleカレンダー埋込 | 実装コストほぼゼロ |

---

## 7. 推奨する「次の一歩」

1. **昇降級グラフを実装**：`data/confirmed` を集計し `LeagueTrend.astro` を追加。**追加データ収集ゼロ・即・映える**。以降のグラフ実装の型にもなる。
2. **社団戦パイプラインを試作**：最新回（第34回/令和7年度）の順位一覧＋ランキングPDFで「抽出→`data/shadan/confirmed/R07.json`→`/shadan/` 表示」を通しで検証（**立教大学紫龍会・紫龍会の2チーム対応**）。`schema.json` に `shadan_pdf` を追加。
3. **限定コンテンツ基盤を開設**：共用Googleアカウント＋Googleグループ＋共有フォルダを作り、入退会フロー・棚卸し時期を決める（§4-3）。サイト側の実装は入口リンク1つのみ。個人レーティングの公開範囲（§2-2）もここで決める。
4. **これらが固まったら requirements.md に反映**：本ロードマップの確定分は §0 実装状況・§5 定義・§6 機能要件へ落とし込み、ROADMAPは「未確定の構想置き場」として運用。

---

*本ドキュメントは方針検討用のたたき台。優先順位・秘匿レベル・技術選定は体制に合わせて調整する。確定事項は requirements.md へ。*
